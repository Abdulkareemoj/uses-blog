---
import BaseHead from '../components/BaseHead.astro';
import Body from '../components/Body.astro';
import Content from '../components/Content.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import Masthead from '../components/Masthead.astro';
import { SITE_DESCRIPTION,SITE_TITLE } from '../config';

const posts = (await Astro.glob('./posts/*.md')).sort((a: any, b: any) => {
  return new Date(b.formatter.pubDate).valueOf() - new Date(a.formatter.pubDate).valueOf();
});
 
---

<!DOCTYPE html>
<!-- <html lang="en"> -->
<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>

	
	<Body />
		
		<Header title={SITE_TITLE} />
		<main class="pt-[56px]">
		<Masthead />
			<h1 class="text-center">
				What i use
			</h1>
			<Content>
				<section>
					<p class="mb-8 indent-4">
Lorem ipsum dolor, sit amet consectetur adipisicing elit. Sint labore obcaecati nihil doloremque dicta quidem praesentium consectetur quos tenetur facilis inventore, distinctio optio dolor laborum at nulla temporibus vero dolores accusamus molestiae perspiciatis beatae ipsum provident. Culpa temporibus perferendis earum voluptatibus? Fugit nobis recusandae ad quisquam tenetur deserunt distinctio perferendis.
				<a class="inline-flex items-center gap-1 indent-1 text-orange-500" href="http://">Learn More</a>	
</p>
				</section>
				<section>
					<ul class="grid grid-cols-1 md:grid-cols-2 gap-4">{
						posts.map(post =>
						<li class="text-center mb-4"> <a href={post.url}>
							<div class="mt-3 text-xl">{post.frontmatter.title}
								<div/>
						<div class="opacity-70">		{post.frontmatter.description}	
							</div><a/>
						<li/>
						
						<ul/>
					<section/>
			</Content>
		</main>
		<Footer />
	</Body>
</html>